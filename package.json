{
  "name": "purity-command-tower",
  "version": "0.1.0",
  "private": true,
  "description": "Babylon x Purity Total Available Inventory, Match-Rate Predictor & Procurement Planner",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "scripts/discover"
  ],
  "scripts": {
    "discover": "npm run start --workspace=@pct/discover",
    "dev:api": "npm run dev --workspace=@pct/api",
    "dev:web": "npm run dev --workspace=@pct/web",
    "dev": "concurrently -k -n api,web -c blue,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "build": "npm run build --workspace=@pct/web && npm run build --workspace=@pct/api",
    "typecheck": "npm run typecheck --workspace=@pct/engine && npm run typecheck --workspace=@pct/api && npm run typecheck --workspace=@pct/web",
    "test": "npm run test --workspace=@pct/engine && npm run test --workspace=@pct/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
