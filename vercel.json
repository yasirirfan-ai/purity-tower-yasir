{
  "version": 2,
  "buildCommand": "npm run build --workspace=@pct/web",
  "outputDirectory": "packages/web/dist",
  "installCommand": "npm install --include=dev",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
